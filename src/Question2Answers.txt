1)  a) The method that is being called is call(int) in the Dog class. 

    b) The method that is being called is toString() in the Cat class.

    c) This causes an error because there is no call method in the Object class.

    d) The method that is being called is toString() in the Object class.

    e) The method that is being called is call() in the Dog class.

    f) The method that is being called is toString(String) in the Cat class.

    h) The method that is being called is feet() that was inherited from the Pet class.

    i) This causes an error because Pet doesn't have a call(int) method.
